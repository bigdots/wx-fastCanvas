export interface CanvasParam {
  id: string, //必须，<canvas>标签的id
  UIwidth: number, // 必须，设计稿的宽度，比如750
  width: number, // 必须，canvas的宽度
  height: number
}

export interface Canvas {
  id: string, //必须，<canvas>标签的id
  UIwidth: number, // 必须，设计稿的宽度，比如750
  width: number, // 必须，canvas的宽度
  height: number
}